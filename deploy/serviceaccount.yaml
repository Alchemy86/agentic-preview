# The identity agentic-preview presents to the traffic-manager. Its projected
# token is the only credential the service holds; it needs no Kubernetes API
# access of its own.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agentic-preview
  namespace: agentic-preview
  labels:
    app: agentic-preview
